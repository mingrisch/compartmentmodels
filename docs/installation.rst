============
Installation
============

At the command line::

    $ easy_install compartmentmodels

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv compartmentmodels
    $ pip install compartmentmodels